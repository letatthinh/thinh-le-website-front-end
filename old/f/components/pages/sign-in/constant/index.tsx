import ButtonPropertyType from '@/types/properties/buttons'

const googleSignInButton: ButtonPropertyType = {
  className: 'bg-google-logo',
  label: 'Google sign in button'
}

const signInConstant = {
  googleSignInButton
}

export default signInConstant
